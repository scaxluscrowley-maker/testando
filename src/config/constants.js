export const GAME = {
  // Preparação (Terminal antes da luta)
  MAX_TRIES_PER_BOSS: 7,

  // Combate (Terminal durante a luta - somente consumíveis)
  MAX_BATTLE_CONSUMABLE_CODES: 3,

  MAX_CONSUMABLES: 4,
  EVA_CAP_PP: 60,
  CRIT_CAP_PP: 70,
  DOT_TICK_TIMING: "END_ROUND", // conforme decisão
  ROUNDING: "HALF_UP",
};
